{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Extension: playground",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/vscode", "${workspaceFolder}/playground"],
      "preLaunchTask": "build plugin"
    }
  ]
}
